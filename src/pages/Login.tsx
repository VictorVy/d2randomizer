import LoginCard from "../components/LoginCard";

const Login = () => {
    return (
        <div
            className="grid place-items-center
                        bg-red-500
                        w-screen h-screen"
        >
            <LoginCard />
        </div>
    );
};

export default Login;
